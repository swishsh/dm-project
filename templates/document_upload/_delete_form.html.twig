<form method="post" action="{{ path('document_upload_delete', {'id': document_upload.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document_upload.id) }}">
    <button class="btn">Delete</button>
</form>
